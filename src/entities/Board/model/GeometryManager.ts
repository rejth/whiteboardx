// Geometry Manager is responsible for abstract low level logic and describes handlers for document.addEventListener
// Handlers should calculate coordinates, position of element or something else and dispatch user events to Event Bus.

class GeometryManager {}

export const eventManager = new GeometryManager();
